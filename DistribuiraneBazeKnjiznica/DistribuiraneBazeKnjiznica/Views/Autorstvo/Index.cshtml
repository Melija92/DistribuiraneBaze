@model IEnumerable<DistribuiraneBazeKnjiznica.Models.Autorstvo>

@{
    ViewBag.Title = "Autorstva";
}

<h2>Autorstva</h2>

<p>
    @Html.ActionLink("Dodaj novo autorstvo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Autor.OIB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Knjiga.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UdioAutorstva)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Autor.OIB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Knjiga.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UdioAutorstva)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.AutorstvoID }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.AutorstvoID }) |
            @Html.ActionLink("Izbriši", "Delete", new { id=item.AutorstvoID })
        </td>
    </tr>
}

</table>
